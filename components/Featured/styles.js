import styled from "styled-components";

export const Featured = styled.section`
    margin-top: 2rem;
    margin-bottom: 6rem;

    .container_featured {
        margin: 2rem 10px 0 10px;
        padding: 0 10px;
        display: flex;
        flex-wrap: nowrap;
        overflow-x: scroll;
        gap: 1rem;
    }
     
`; 